package {{ .Name }}

func eval(f *[SysT + 1]gf, a gf) gf {
	r := f[SysT]
	for i := SysT - 1; i >= 0; i-- {
		r = gfMul(r, a)
		r = gfAdd(r, f[i])
	}
	return r
}

func root(out *[SysN]gf, f *[SysT + 1]gf, l *[SysN]gf) {
	for i := 0; i < SysN; i++ {
		out[i] = eval(f, l[i])
	}
}
